# Slide 100: 시나리오 1 디버깅 (작업 흐름)

**Part 5: QUICK START**

## Code Blocks

### Claude analysis

```
Claude의 작업 진행 (사용자 승인 없이 Read는 자동 실행):

  • Read src/pages/user/profile.tsx (line 70-90)
    → preferences 점(.) 접근 (null 체크 없음)
  • Grep "user.preferences" → 3 callsites 발견
  • Read prisma/migrations/..._add_preferences.sql
    → DEFAULT 값 없이 ALTER ADD COLUMN
  • Read src/lib/userService.ts → fallback 없음

Plan: 옵셔널 체이닝 + fallback + 데이터 마이그레이션
```

## Speaker Notes

Claude가 자동으로 진행하는 작업 흐름을 살펴봅니다.
5단계로 진행되는데, 1단계 문제 파일을 Read로 직접 읽고 78행 주변을 분석합니다.
preferences를 점 접근하고 있고 null 체크가 없다는 것을 확인합니다.
2단계 같은 패턴이 다른 곳에도 있는지 Grep으로 확인해 3개 호출 지점을 식별합니다.
3단계 최근 추가된 마이그레이션 SQL을 읽어 DEFAULT 값 없이 컬럼이 추가되었음을 확인합니다.
4단계 userService에서 기본값 매핑 fallback이 있는지도 확인합니다.
5단계 종합한 결과를 Plan으로 제시하는데, 코드 수정뿐 아니라 기존 데이터 마이그레이션까지 포함합니다.
사용자가 Plan에 동의하면 Edit과 Bash로 실제 수정과 테스트를 진행합니다.
