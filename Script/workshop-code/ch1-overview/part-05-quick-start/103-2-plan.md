# Slide 103: 시나리오 2 기능 추가 (Plan 모드)

**Part 5: QUICK START**

## Code Blocks

### Plan

```
> Shift+Tab (Plan 모드 활성화)
[Plan mode: ON. Claude will plan first and ask for approval.]

Claude:
  Plan for cursor-based pagination

  Files to modify:
    1. src/app/api/users/route.ts - 핸들러 수정
    2. src/lib/pagination.ts - 신규 헬퍼 (encode/decode)
    3. src/lib/users.ts - 쿼리 함수 시그니처 변경

  Tests to add:
    1. tests/api/users.pagination.test.ts
       - 기본 페이지 크기
       - limit 경계값 (0, 100, 101)
       - cursor 정확도
       - Link 헤더 형식
       - 빈 결과

  Database
    - 인덱스 점검: (created_at DESC, id DESC) 이미 존재
    - 추가 마이그레이션 불필요

  Approve? (y/n/edit)
```

## Speaker Notes

Plan 모드를 활성화하면 Claude가 먼저 전체 계획을 세우고 사용자 승인을 받은 후 실행에 들어갑니다.
Shift+Tab으로 모드를 토글하고, Claude는 페이지네이션 구현 계획을 제시합니다.
수정할 파일 3개, 추가할 테스트 5가지 시나리오, 데이터베이스 측 점검까지 포함됩니다.
인덱스가 이미 적절히 존재하므로 추가 마이그레이션이 불필요하다는 것까지 확인했습니다.
사용자는 y로 승인하거나, n으로 거부하거나, edit로 계획을 수정할 수 있습니다.
Plan 모드는 큰 변경 작업에서 실수와 재작업을 크게 줄여 줍니다.
